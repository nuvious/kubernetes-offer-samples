apiVersion: v1
kind: Service
metadata:
  name: {{ include "test-subchart-redis.fullname" . }}
  labels:
    {{- include "test-subchart-redis.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "test-subchart-redis.selectorLabels" . | nindent 4 }}
